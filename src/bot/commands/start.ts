/**
 * /start command handler
 */
import { CommandContext } from 'grammy';
import { logger } from '../../utils/logger.js';

export async function startCommand(ctx: CommandContext<any>) {
  const userId = ctx.from?.id;
  logger.info('Start command', { userId });

  const message =
    '👋 **Менеджер публикаций**\n\n' +
    'Добро пожаловать! Этот бот помогает управлять отложенными публикациями в ваших Telegram-каналах.\n\n' +
    '**Основные возможности:**\n' +
    '• Создание и редактирование публикаций\n' +
    '• Планирование времени публикации\n' +
    '• Работа с медиафайлами и кнопками\n' +
    '• Просмотр аналитики по постам\n' +
    '• Управление несколькими каналами\n\n' +
    'Используйте /help для просмотра всех команд.';

  await ctx.reply(message, { parse_mode: 'Markdown' });
}
