# Спецификация: Пользовательские сценарии

## Сценарий 1: Первый запуск бота

### Шаги:
1. Администратор открывает бота и нажимает START
2. Бот проверяет telegram_id администратора
3. Бот отправляет приветственное сообщение с основным меню

### Ожидаемый результат:
```
Менеджер публикаций

Бот для управления отложенными публикациями в Telegram-каналах.

Основные возможности:
• Создание и редактирование публикаций
• Планирование времени публикации
• Аналитика по опубликованным постам

Используйте /help для просмотра доступных команд
```
**Кнопки:** `[📝 Создать публикацию]` `[📋 Мои каналы]` `[📊 Статистика]`

### Что должно сохраниться в БД:
- telegram_id администратора
- username
- first_name, last_name
- created_at (время первого запуска)
- timezone (определяется автоматически или устанавливается по умолчанию UTC+3)
- last_activity_at

---

## Сценарий 2: Создание новой публикации

### Предусловия:
- Администратор авторизован (telegram_id совпадает с ADMIN_TELEGRAM_ID)
- В системе есть хотя бы один добавленный канал

### Основной flow:

#### Шаг 1: Выбор канала
1. Администратор вводит команду `/newpost`
2. Бот показывает список добавленных каналов
3. Администратор выбирает канал
4. Система сохраняет выбранный канал в контексте conversation

#### Шаг 2: Ввод текста публикации
1. Бот запрашивает текст публикации
2. Администратор вводит текст (с HTML-форматированием)
3. Бот валидирует длину текста (максимум 4096 символов)
4. Бот показывает количество символов и предлагает добавить медиа

#### Шаг 3: Добавление медиафайлов (опционально)
1. Администратор выбирает "Добавить медиа" или "Пропустить"
2. Если выбрано "Добавить медиа":
   - Администратор отправляет фото, видео или документы (до 10 файлов)
   - Бот сохраняет file_id каждого файла
   - Бот показывает счётчик: "Текущие файлы: N / 10"
   - Администратор нажимает "Готово"
3. Система сохраняет медиа в контексте conversation

#### Шаг 4: Добавление кнопок (опционально)
1. Бот предлагает добавить inline-кнопки под публикацией
2. Если администратор выбирает "Добавить кнопки":
   - Администратор вводит данные в формате: `Текст кнопки | https://url.com`
   - Бот валидирует формат и URL
   - Можно добавить несколько кнопок
   - Администратор нажимает "Готово"
3. Система сохраняет структуру кнопок в контексте conversation

#### Шаг 5: Выбор времени публикации
1. Бот предлагает выбрать: "Сейчас", "Запланировать" или "Сохранить как черновик"
2. Если выбрано "Запланировать":
   - Бот показывает inline-календарь (месячный вид)
   - Администратор выбирает дату
   - Бот показывает выбор часа (00:00 - 23:00)
   - Администратор выбирает час
   - Бот показывает выбор минут (00, 15, 30, 45)
   - Администратор выбирает минуты
   - Бот валидирует, что время не в прошлом
3. Если выбрано "Сейчас" - время устанавливается как текущее
4. Если выбрано "Сохранить как черновик" - статус устанавливается как 'draft', время null

#### Шаг 6: Предпросмотр и подтверждение
1. Бот показывает полное превью публикации:
   - Канал
   - Время публикации
   - Текст с форматированием
   - Количество медиафайлов
   - Количество кнопок
2. Администратор выбирает "Подтвердить", "Редактировать" или "Отмена"
3. Если "Подтвердить":
   - Система создаёт запись в таблице `posts`
   - Система создаёт записи в таблице `media` (если есть медиа)
   - Система создаёт записи в таблице `post_buttons` (если есть кнопки)
   - Система создаёт запись в таблице `schedules` (если запланировано)
   - Если "Сейчас" - система сразу публикует в канал
   - Если запланировано - система добавляет в очередь планировщика

### Альтернативные сценарии:

**Нет добавленных каналов:**
- Бот показывает сообщение: "У вас нет добавленных каналов"
- Предлагает кнопку "Управление каналами" (/channels)

**Превышен лимит символов:**
- Бот показывает: "Текущая длина: [N] символов. Максимум: 4096. Сократите на [N] символов"
- Администратор редактирует текст

**Неверный формат кнопки:**
- Бот показывает пример правильного формата
- Администратор вводит заново

**Попытка запланировать на прошедшее время:**
- Бот показывает предупреждение: "Невозможно запланировать на прошедшее время"
- Предлагает выбрать другое время

**Отмена действия:**
- Администратор вводит /cancel в любой момент
- Conversation завершается, данные не сохраняются
- Бот показывает: "Действие отменено"

**Таймаут (5 минут без ответа):**
- Conversation автоматически завершается
- Бот показывает: "Время ожидания истекло. Создание публикации отменено"

### Пример диалога:
```
Администратор: /newpost

Бот: 📝 Создание публикации
     Выберите канал для публикации:
     [📢 Мой канал новостей]
     [📢 Канал о технологиях]

Администратор: [нажимает "Мой канал новостей"]

Бот: ✅ Канал выбран: Мой канал новостей

     Введите текст публикации:
     Поддерживается HTML-форматирование
     Для отмены: /cancel

Администратор: <b>Новая статья</b> о технологиях уже доступна!
               Читайте по ссылке: <a href="https://example.com">тут</a>

Бот: ✅ Текст сохранен
     Символов: 87 / 4096

     Добавить медиафайлы?
     [📎 Добавить медиа] [⏭️ Пропустить]

Администратор: [отправляет фото]

Бот: 📎 Добавление медиафайлов
     Текущие файлы: 1 / 10
     [✅ Готово] [❌ Отмена]

Администратор: [нажимает "Готово"]

Бот: ✅ Медиа добавлены: 1 файлов
     Добавить кнопки под публикацией?
     [🔘 Добавить кнопки] [⏭️ Пропустить]

Администратор: [нажимает "Пропустить"]

Бот: 🕐 Время публикации
     Когда опубликовать?
     [🚀 Сейчас] [⏰ Запланировать]
     [💾 Сохранить как черновик] [❌ Отмена]

Администратор: [нажимает "Запланировать"]

Бот: 📅 Выбор даты публикации
     [Показывает календарь]

Администратор: [выбирает 15 февраля]

Бот: ✅ Дата выбрана: 15 февраля (четверг)
     Выберите время публикации:
     [00:00] [01:00] ... [23:00]

Администратор: [выбирает 10:00]

Бот: ✅ Час выбран: 10:00
     Уточните минуты:
     [10:00] [10:15] [10:30] [10:45]

Администратор: [выбирает 10:00]

Бот: 📋 Предпросмотр публикации

     Канал: Мой канал новостей
     Время публикации: 15 февраля (четверг), 10:00

     Текст:
     **Новая статья** о технологиях уже доступна!
     Читайте по ссылке: тут

     Медиа: 1 файлов
     Кнопки: 0 кнопок

     Всё верно?
     [✅ Подтвердить] [✏️ Редактировать] [❌ Отмена]

Администратор: [нажимает "Подтвердить"]

Бот: ✅ Публикация запланирована

     Канал: Мой канал новостей
     Дата и время: 15 февраля (четверг), 10:00

     Публикация будет отправлена автоматически
     [📅 Календарь] [📝 Создать еще]
```

### Что сохраняется в БД:

**Таблица `posts`:**
- channel_id
- text (с HTML-форматированием)
- status ('scheduled')
- created_at
- updated_at

**Таблица `media`:**
- post_id
- file_id (от Telegram)
- file_type ('photo', 'video', 'document')
- file_size
- position (порядок в media group)

**Таблица `post_buttons`:**
- post_id
- text (текст кнопки)
- url
- row (номер ряда)
- position (позиция в ряду)

**Таблица `schedules`:**
- post_id
- scheduled_at (15 февраля, 10:00)
- status ('pending')

**Таблица `action_logs`:**
- user_id
- action ('post_created')
- post_id
- created_at

---

## Сценарий 3: Редактирование публикации

### Предусловия:
- Администратор авторизован
- В системе есть публикации (черновики или запланированные)

### Основной flow:

1. Администратор вводит команду `/editpost`
2. Бот показывает список публикаций (с пагинацией, по 5 на страницу)
3. Администратор выбирает публикацию
4. Бот показывает меню редактирования с кнопками:
   - 📢 Канал
   - 📝 Текст
   - 📎 Медиа
   - 🔘 Кнопки
   - 🕐 Время
   - 👁️ Превью
   - ✅ Сохранить
   - ❌ Отмена
5. Администратор выбирает что редактировать
6. В зависимости от выбора, бот запускает соответствующий подсценарий:

#### Редактирование канала:
- Показывает список каналов
- Администратор выбирает новый канал
- Возврат в меню редактирования

#### Редактирование текста:
- Бот показывает текущий текст
- Администратор вводит новый текст
- Бот валидирует длину
- Возврат в меню редактирования

#### Редактирование медиа:
- Бот показывает текущие медиафайлы
- Администратор может:
  - Добавить новые файлы
  - Удалить существующие
  - Изменить порядок
- Возврат в меню редактирования

#### Редактирование кнопок:
- Бот показывает текущие кнопки
- Администратор может:
  - Добавить новые кнопки
  - Удалить существующие
  - Изменить URL или текст
- Возврат в меню редактирования

#### Редактирование времени:
- Бот показывает текущее время публикации
- Показывает календарь (как при создании)
- Администратор выбирает новую дату и время
- Бот валидирует время
- Возврат в меню редактирования

7. После всех изменений администратор нажимает "Сохранить"
8. Система обновляет запись в БД
9. Система обновляет расписание в планировщике (если изменилось время)
10. Бот показывает: "✅ Публикация обновлена"

### Альтернативные сценарии:

**Нет публикаций для редактирования:**
- Бот показывает: "У вас нет публикаций для редактирования"

**Редактирование уже опубликованной публикации:**
- Бот показывает предупреждение: "Публикация уже опубликована. Редактирование недоступно"
- Предлагает только просмотр статистики

**Отмена редактирования без сохранения:**
- Администратор нажимает "Отмена"
- Бот показывает подтверждение: "Изменения не сохранены. Отменить редактирование?"
- Если "Да" - изменения отменяются

### Что обновляется в БД:

**Таблица `posts`:**
- Соответствующие поля (text, channel_id, и т.д.)
- updated_at (обновляется автоматически)

**Таблица `schedules`:**
- scheduled_at (если изменилось время)

**Таблица `action_logs`:**
- Новая запись с action='post_updated'

---

## Сценарий 4: Работа с календарём

### Предусловия:
- Администратор авторизован
- В системе есть запланированные публикации

### Основной flow:

1. Администратор вводит команду `/schedule`
2. Бот показывает главное меню календаря:
   - 📆 По дням
   - 📊 По неделям
   - 🗓️ По месяцам
   - 🔍 Фильтры

#### Просмотр по дням:
1. Администратор выбирает "По дням"
2. Бот показывает публикации на сегодня (список по времени)
3. Администратор может:
   - Перелистывать дни (кнопки "◀️ Пред день" / "След день ▶️")
   - Нажать на публикацию для просмотра деталей
4. При клике на публикацию показывается меню:
   - 👁️ Превью
   - ✏️ Редактировать
   - 🔄 Перенести
   - 📋 Дублировать
   - 🗑️ Удалить
   - 🔙 Назад

#### Просмотр по неделям:
1. Администратор выбирает "По неделям"
2. Бот показывает 7 дней с количеством публикаций каждый день
3. Администратор может:
   - Перелистывать недели
   - Выбрать конкретный день для детального просмотра

#### Просмотр по месяцам:
1. Администратор выбирает "По месяцам"
2. Бот показывает календарную сетку месяца
3. Под каждой датой указано количество публикаций
4. Администратор может:
   - Перелистывать месяцы
   - Нажать на дату для просмотра публикаций в этот день

#### Фильтры:
1. Администратор выбирает "Фильтры"
2. Бот показывает опции:
   - По каналу (выбор конкретного канала)
   - По статусу (запланировано / опубликовано / ошибка)
3. После применения фильтра показываются только соответствующие публикации

#### Перенос публикации:
1. Администратор выбирает публикацию → "🔄 Перенести"
2. Бот показывает текущее время публикации
3. Показывается календарь (как при создании)
4. Администратор выбирает новую дату и время
5. Бот запрашивает подтверждение
6. Система обновляет время в БД и планировщике
7. Бот показывает: "✅ Публикация перенесена на [новая дата и время]"

#### Дублирование публикации:
1. Администратор выбирает публикацию → "📋 Дублировать"
2. Система создаёт копию публикации (текст, медиа, кнопки)
3. Бот предлагает выбрать канал (может быть тот же или другой)
4. Бот предлагает выбрать время публикации
5. Показывается превью
6. После подтверждения создаётся новая публикация в БД
7. Бот показывает: "✅ Публикация дублирована"

### Что происходит в БД:

**При переносе:**
- Обновляется `schedules.scheduled_at`
- Добавляется запись в `action_logs` (action='post_rescheduled')

**При дублировании:**
- Создаётся новая запись в `posts`
- Копируются записи из `media`
- Копируются записи из `post_buttons`
- Создаётся запись в `schedules`
- Добавляется запись в `action_logs` (action='post_duplicated')

---

## Сценарий 5: Просмотр статистики

### Предусловия:
- Администратор авторизован
- В системе есть опубликованные публикации

### Основной flow:

1. Администратор вводит команду `/statistics`
2. Бот показывает общую статистику:
   - Всего опубликовано
   - Запланировано
   - Черновиков
   - Среднее просмотров
   - Среднее репостов
   - Время последнего обновления
3. Кнопки:
   - 📈 По каналам
   - 📅 За период (опционально, в будущем)
   - 🔄 Обновить

#### Статистика по каналам:
1. Администратор выбирает "По каналам"
2. Бот показывает список каналов с количеством публикаций
3. Администратор выбирает канал
4. Бот показывает детальную статистику канала:
   - Опубликовано / Запланировано
   - Всего просмотров / репостов
   - Среднее на публикацию
   - Топ-3 публикации по просмотрам
5. Администратор может выбрать конкретную публикацию для детальной статистики

#### Статистика публикации:
1. Бот показывает:
   - Канал
   - Дата публикации
   - Просмотры
   - Репосты
   - Реакции (если доступно через API)
   - Время последнего обновления
   - Ссылка на публикацию в канале
2. Кнопка "🔄 Обновить" для получения свежей статистики

#### Обновление статистики:
1. Администратор нажимает "Обновить"
2. Бот запрашивает свежие данные из Telegram API
3. Обновляет запись в таблице `post_statistics`
4. Показывает обновлённые данные

### Что происходит в БД:

**При обновлении статистики:**
- Обновляется `post_statistics.views_count`
- Обновляется `post_statistics.forwards_count`
- Обновляется `post_statistics.reactions_count` (если доступно)
- Обновляется `post_statistics.last_updated_at`

---

## Сценарий 6: Управление каналами

### Предусловия:
- Администратор авторизован

### Основной flow:

#### Добавление канала:
1. Администратор вводит команду `/channels`
2. Бот показывает список добавленных каналов
3. Администратор нажимает "+ Добавить канал"
4. Бот запрашивает username канала или пересылку сообщения
5. Администратор отправляет @channel_username или пересылает сообщение
6. Бот пытается получить информацию о канале через API:
   - Проверяет, что бот добавлен в канал
   - Проверяет, что бот имеет права администратора
   - Проверяет право на публикацию сообщений
7. Если всё успешно:
   - Система создаёт запись в таблице `channels`
   - Бот показывает: "✅ Канал добавлен"
8. Если ошибка:
   - Бот показывает подробное сообщение с инструкцией

#### Проверка доступа к каналу:
1. Администратор выбирает канал → "✏️ Проверить доступ"
2. Бот проверяет права через Telegram API
3. Показывает результат:
   - ✅ Доступ подтверждён (всё в порядке)
   - ⚠️ Нет доступа (с описанием проблемы)

#### Удаление канала:
1. Администратор выбирает канал → "🗑️ Удалить канал"
2. Бот показывает предупреждение:
   - Количество публикаций в этом канале
   - "Это действие нельзя отменить"
3. Администратор подтверждает удаление
4. Система помечает канал как неактивный (is_active = false)
5. Публикации в этом канале не удаляются, но новые создать нельзя

### Что происходит в БД:

**При добавлении канала:**
- Создаётся запись в `channels`:
  - channel_id (от Telegram)
  - channel_username
  - channel_title
  - is_active (true)
  - date_added

**При удалении канала:**
- Обновляется `channels.is_active = false`
- Добавляется запись в `action_logs` (action='channel_removed')

---

## Сценарий 7: Работа с ошибками

### Типичные ошибки:

#### Ошибка 1: Неавторизованный пользователь
```
Пользователь (не админ): /start
Бот: 🚫 Доступ запрещён
     Только администратор может использовать этого бота
```

#### Ошибка 2: Канал недоступен при публикации
```
Планировщик пытается опубликовать в 10:00
Канал недоступен (бот удалён или отозваны права)

Система:
1. Помечает публикацию как 'failed' в schedules
2. Отправляет уведомление администратору:

Бот: ❌ Ошибка публикации

     Не удалось опубликовать запланированную публикацию

     Канал: [Название канала]
     Запланированное время: 14 февраля (среда), 10:00

     Причина: Бот не имеет прав в канале

     Действия:
     [🔄 Повторить публикацию]
     [✏️ Редактировать]
     [🗑️ Удалить]
```

#### Ошибка 3: База данных недоступна
```
Администратор: /newpost

Система определяет, что PostgreSQL недоступна

Бот: ⚠️ База данных недоступна

     Бот работает в режиме только для чтения

     Создание и редактирование публикаций временно недоступно

     Администратор получил уведомление об ошибке

Система:
1. Отправляет email администратору
2. Отправляет уведомление в Telegram
3. Логирует ошибку в файл
```

#### Ошибка 4: Критическая ошибка системы
```
Происходит неожиданная ошибка (например, crash бота)

Система:
1. Логирует полный stack trace
2. Отправляет email администратору
3. Отправляет Telegram-уведомление:

Бот: 🚨 Критическая ошибка

     Обнаружена критическая ошибка системы

     Тип: UnhandledException
     Время: 14 февраля (среда), 15:30

     Администратор получил email-уведомление

     Код ошибки: ERR_12345

4. Автоматически перезапускается (Docker restart policy)
```

---

## Чеклист для каждого сценария:
- [x] Описан happy path (успешный сценарий)
- [x] Описаны альтернативные пути
- [x] Описана обработка ошибок
- [x] Указаны права доступа (только администратор)
- [x] Приведены примеры сообщений и диалогов
- [x] Указано, что сохраняется в БД
- [x] Описаны переходы между состояниями
- [x] Учтены граничные случаи (таймауты, отмены, валидации)
